1、为了方便测试，我采用的是自己电脑两个markdown之间的连接，如果要进行不同电脑上的连接，只需把客户端申请连接的ip
地址改为目的PC的IP地址即可。


2、目录下有两个jar包，MarkDownEditor.jar是作为服务器的，打开之后，点击connect setting里面的open server，即
开启了服务器线程。另一个包是TestClient，它是客户端，打开之后，点击connect setting里面的connect，出现connect 
success 之后，即表明两个markdown已经连接成功。